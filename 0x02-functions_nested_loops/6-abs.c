#include "main.h"
/**
 * _abs - entry point
 *
 * Description: compute absolute value
 *@n: the integer to pass
 * return the absolute value
 */
int _abs(int n)
{
if (n < 1)
{
return (n * (-1));
}
else
{
return (n);
}
}
